# 🚀 Быстрый старт - Пошаговая инструкция

## Шаг 1: Проверка установки Docker

Убедитесь, что Docker Desktop установлен и запущен:

```powershell
docker --version
docker-compose --version
```

Если команды не работают, установите [Docker Desktop для Windows](https://www.docker.com/products/docker-desktop/).

## Шаг 2: Переход в директорию проекта

```powershell
cd D:\!TWELVEDEVS\custom_document_recognition
```

Или если вы уже в правильной директории:
```powershell
pwd
```

## Шаг 3: Проверка файлов

Убедитесь, что файлы на месте:

```powershell
ls docker-compose.yml
ls backend/Dockerfile
ls frontend/Dockerfile
```

## Шаг 4: Запуск всех сервисов

Запустите все контейнеры в фоновом режиме:

```powershell
docker-compose up -d
```

Эта команда:
- Соберет образы для backend и frontend (если еще не собраны)
- Запустит MongoDB
- Запустит Ollama
- Запустит Backend
- Запустит Frontend

**Время первого запуска:** 5-10 минут (зависит от скорости интернета для загрузки образов)

## Шаг 5: Проверка статуса контейнеров

Проверьте, что все контейнеры запущены:

```powershell
docker-compose ps
```

Должны быть запущены 4 контейнера:
- `doc-recognition-mongodb`
- `doc-recognition-ollama`
- `doc-recognition-backend`
- `doc-recognition-frontend`

## Шаг 6: Просмотр логов (опционально)

Если что-то не работает, проверьте логи:

```powershell
# Все логи
docker-compose logs

# Логи конкретного сервиса
docker-compose logs backend
docker-compose logs frontend
docker-compose logs mongodb
docker-compose logs ollama

# Логи в реальном времени
docker-compose logs -f
```

## Шаг 7: Загрузка модели Mistral для Ollama

После первого запуска загрузите модель LLM:

```powershell
docker exec doc-recognition-ollama ollama pull mistral
```

**Важно:** Это может занять 5-15 минут в зависимости от скорости интернета (модель ~4GB).

Проверьте, что модель загружена:

```powershell
docker exec doc-recognition-ollama ollama list
```

Должна быть видна модель `mistral`.

## Шаг 8: Проверка работы сервисов

### Frontend
Откройте в браузере: http://localhost:3000

### Backend API
Проверьте в браузере или через curl:
```powershell
curl http://localhost:4000/api/ping
```

Или откройте в браузере: http://localhost:4000/api/ping

Должен вернуться: `{"message":"Server is alive"}`

### MongoDB
Проверьте подключение:
```powershell
docker exec doc-recognition-mongodb mongosh --eval "db.adminCommand('ping')"
```

### Ollama
Проверьте доступность:
```powershell
curl http://localhost:11434/api/tags
```

## Шаг 9: Использование приложения

1. Откройте http://localhost:3000 в браузере
2. Загрузите документ (PDF, PNG, JPG)
3. Выберите режим (автоматический или ручной)
4. Запустите распознавание

## Полезные команды

### Остановка всех сервисов
```powershell
docker-compose down
```

### Остановка с удалением volumes (удалит данные БД и модели)
```powershell
docker-compose down -v
```

### Перезапуск конкретного сервиса
```powershell
docker-compose restart backend
docker-compose restart frontend
```

### Пересборка после изменений в коде
```powershell
# Пересобрать backend
docker-compose build backend
docker-compose up -d backend

# Пересобрать frontend
docker-compose build frontend
docker-compose up -d frontend

# Пересобрать всё
docker-compose build
docker-compose up -d
```

### Просмотр использования ресурсов
```powershell
docker stats
```

### Вход в контейнер (для отладки)
```powershell
# В backend контейнер
docker exec -it doc-recognition-backend sh

# В MongoDB
docker exec -it doc-recognition-mongodb mongosh

# В Ollama
docker exec -it doc-recognition-ollama sh
```

## Troubleshooting

### Проблема: Порты заняты

Если порты 3000, 4000, 27017 или 11434 заняты, измените их в `docker-compose.yml`:

```yaml
ports:
  - "3001:80"  # вместо 3000:80 для frontend
  - "4001:4000"  # вместо 4000:4000 для backend
```

### Проблема: Контейнер не запускается

1. Проверьте логи: `docker-compose logs <service-name>`
2. Проверьте, что Docker Desktop запущен
3. Убедитесь, что достаточно места на диске
4. Проверьте, что порты свободны

### Проблема: Backend не может подключиться к MongoDB

1. Убедитесь, что MongoDB контейнер запущен: `docker-compose ps`
2. Проверьте логи MongoDB: `docker-compose logs mongodb`
3. Дождитесь завершения healthcheck (может занять 30-60 секунд)

### Проблема: Ollama модель не загружается

1. Проверьте интернет-соединение
2. Убедитесь, что достаточно места на диске (минимум 10GB свободно)
3. Попробуйте загрузить модель снова: `docker exec doc-recognition-ollama ollama pull mistral`

### Проблема: Frontend не может подключиться к Backend

1. Проверьте, что backend запущен: `docker-compose ps`
2. Проверьте переменную `VITE_API_BASE` в `docker-compose.yml`
3. Откройте консоль браузера (F12) и проверьте ошибки

### Очистка и перезапуск с нуля

Если что-то пошло не так, можно начать заново:

```powershell
# Остановить и удалить всё
docker-compose down -v

# Удалить образы (опционально)
docker-compose rm -f

# Пересобрать и запустить
docker-compose build
docker-compose up -d
```

## Структура проекта в Docker

```
┌─────────────────────────────────────┐
│  Docker Network:                    │
│  doc-recognition-network            │
│                                     │
│  ┌──────────────┐                  │
│  │  Frontend    │ :3000            │
│  │  (nginx)     │                  │
│  └──────┬───────┘                  │
│         │                           │
│  ┌──────▼───────┐                  │
│  │  Backend     │ :4000            │
│  │  (Node.js)   │                  │
│  └──┬───────┬───┘                  │
│     │       │                       │
│  ┌──▼───┐ ┌▼────────┐             │
│  │MongoDB│ │ Ollama  │             │
│  │:27017│ │ :11434  │             │
│  └──────┘ └─────────┘             │
└─────────────────────────────────────┘
```

## Следующие шаги

После успешного запуска:
1. ✅ Приложение доступно на http://localhost:3000
2. ✅ Все сервисы работают локально
3. ✅ Данные сохраняются в Docker volumes
4. ✅ Модель Ollama загружена и готова к работе

Для деплоя в продакшн см. [DEPLOYMENT.md](./DEPLOYMENT.md)

