# Быстрый старт: Выгрузка на GitHub и деплой на Railway

## 📋 Что нужно сделать

### 1️⃣ Выгрузка на GitHub

Выполните команды из файла `GIT_COMMANDS.md` или используйте следующие:

```bash
# Переход в директорию backend
cd custom_document_recognition/backend

# Инициализация Git
git init
git add .
git commit -m "Initial commit: Document Recognition Backend"

# Создайте репозиторий на GitHub, затем:
git remote add origin https://github.com/your-username/your-repo-name.git
git branch -M main
git push -u origin main
```

### 2️⃣ Деплой на Railway

1. Зайдите на [Railway](https://railway.app) и войдите через GitHub
2. Нажмите **"New Project"** → **"Deploy from GitHub repo"**
3. Выберите ваш репозиторий
4. Railway автоматически определит конфигурацию

### 3️⃣ Настройка переменных окружения в Railway

В настройках проекта Railway добавьте переменные:

**Обязательные:**
- `MONGO_URI` - URI вашей MongoDB (можно использовать Railway MongoDB плагин)
- `MONGO_DB` - название базы данных

**Рекомендуемые:**
- `ALLOWED_ORIGINS` - домены фронтенда через запятую
- `NODE_ENV=production`

**Опциональные (для LLM):**
- `USE_GROQ=true` и `GROQ_API_KEY` (если используете Groq)
- ИЛИ `OLLAMA_URL` и `OLLAMA_MODEL` (если используете Ollama)

Пример значений см. в `env.example.txt`

### 4️⃣ Проверка

После деплоя Railway предоставит URL. Проверьте:
```
GET https://your-app.up.railway.app/api/ping
```

Должен вернуться: `{"message": "Server is alive"}`

## 📚 Подробные инструкции

- `GIT_COMMANDS.md` - детальные команды для Git
- `DEPLOY.md` - подробная инструкция по деплою на Railway
- `README.md` - общая документация проекта

## ⚠️ Важно

- Не коммитьте файл `.env` (он в `.gitignore`)
- Убедитесь, что `eng.traineddata` и `rus.traineddata` загружены в репозиторий
- Все переменные окружения настройте в Railway dashboard

