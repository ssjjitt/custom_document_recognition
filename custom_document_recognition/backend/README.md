# Document Recognition Backend

Backend сервис для распознавания документов с использованием OCR (Tesseract.js) и LLM для извлечения структурированных данных.

## Возможности

- 📄 Загрузка и обработка PDF и изображений
- 🔍 OCR распознавание текста (поддержка русского и английского языков)
- 🤖 Интеграция с LLM (Groq API или Ollama) для извлечения структурированных данных
- 📝 Управление шаблонами документов
- 📊 История распознаваний
- 💾 Хранение данных в MongoDB

## Технологии

- Node.js 18+
- TypeScript
- Express.js
- Tesseract.js (OCR)
- MongoDB
- Docker

## Установка и запуск

### Локальная разработка

1. Установите зависимости:
```bash
npm install
```

2. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

3. Настройте переменные окружения в `.env`

4. Запустите в режиме разработки:
```bash
npm run dev
```

### Сборка для продакшн

```bash
npm run build
npm start
```

## Переменные окружения

См. `.env.example` для полного списка переменных окружения.

Основные переменные:
- `PORT` - порт сервера (по умолчанию 4000)
- `MONGO_URI` - URI подключения к MongoDB
- `MONGO_DB` - название базы данных
- `ALLOWED_ORIGINS` - разрешенные источники для CORS
- `GROQ_API_KEY` - API ключ для Groq (опционально)
- `OLLAMA_URL` - URL для Ollama (опционально)

## Docker

Сборка образа:
```bash
docker build -t document-recognition-backend .
```

Запуск контейнера:
```bash
docker run -p 4000:4000 --env-file .env document-recognition-backend
```

## Деплой на Railway

1. Создайте новый проект на [Railway](https://railway.app)
2. Подключите GitHub репозиторий
3. Railway автоматически определит конфигурацию из `railway.json` и `railway.toml`
4. Добавьте переменные окружения в настройках проекта Railway
5. Деплой произойдет автоматически при push в репозиторий

### Необходимые переменные окружения в Railway:

- `PORT` (Railway устанавливает автоматически)
- `MONGO_URI` - URI вашей MongoDB (можно использовать Railway MongoDB плагин)
- `MONGO_DB` - название базы данных
- `ALLOWED_ORIGINS` - домены вашего фронтенда
- `GROQ_API_KEY` или `OLLAMA_URL` (если используете LLM)

## API Endpoints

- `GET /api/ping` - проверка работоспособности сервера
- `POST /api/upload` - загрузка файла
- `POST /api/recognize` - распознавание документа
- `GET /api/history` - получение истории распознаваний
- `GET /api/templates` - управление шаблонами
- `POST /api/assist` - помощь с извлечением данных
- `GET /api/pages` - получение страниц документа

## Лицензия

ISC

