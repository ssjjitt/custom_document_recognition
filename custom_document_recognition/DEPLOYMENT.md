# Инструкция по развертыванию

## 🐳 Docker (Рекомендуется для полного стека)

### Быстрый старт

```bash
# 1. Перейдите в директорию проекта
cd custom_document_recognition

# 2. Запустите контейнеры
docker-compose up -d

# 3. Проверьте статус
docker-compose ps

# 4. Просмотр логов
docker-compose logs -f
```

Приложение будет доступно:
- Frontend: http://localhost:3000
- Backend: http://localhost:4000
- MongoDB: localhost:27017
- Ollama: http://localhost:11434

**Важно:** После первого запуска загрузите модель mistral:
```bash
docker exec doc-recognition-ollama ollama pull mistral
```

### Остановка

```bash
docker-compose down
```

### Переменные окружения

Создайте файл `.env` в корне проекта (опционально):

```env
MONGODB_URI=mongodb://localhost:27017/doc-recognition
OLLAMA_URL=http://host.docker.internal:11434/api/generate
VITE_API_BASE=http://localhost:4000
```

Подробнее см. [DOCKER.md](./DOCKER.md)

## ☁️ Vercel (Только Frontend)

### Через Vercel CLI

```bash
# 1. Установите Vercel CLI
npm i -g vercel

# 2. Перейдите в директорию frontend
cd frontend

# 3. Войдите в Vercel
vercel login

# 4. Деплой
vercel

# 5. Production деплой
vercel --prod
```

### Через GitHub Integration

1. Загрузите код в GitHub
2. Зайдите на [vercel.com](https://vercel.com)
3. Нажмите "New Project"
4. Импортируйте репозиторий
5. Настройки:
   - **Root Directory:** `frontend`
   - **Framework Preset:** Vite
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`
6. Добавьте переменную окружения:
   - `VITE_API_BASE` = URL вашего backend API

### Backend для Vercel

Backend нужно развернуть отдельно на платформе поддерживающей Docker:
- **Railway** (рекомендуется): https://railway.app
- **Render**: https://render.com
- **Fly.io**: https://fly.io
- **DigitalOcean App Platform**: https://www.digitalocean.com/products/app-platform

Подробнее см. [VERCEL.md](./VERCEL.md)

## 📋 Чеклист деплоя

### Docker
- [ ] Docker и Docker Compose установлены
- [ ] Создан файл `.env` (опционально, значения по умолчанию настроены)
- [ ] Порты 3000, 4000, 27017 и 11434 свободны
- [ ] После первого запуска загружена модель mistral в Ollama

### Vercel
- [ ] Код загружен в GitHub
- [ ] Backend развернут на отдельной платформе
- [ ] Переменная `VITE_API_BASE` настроена в Vercel
- [ ] CORS настроен на backend для домена Vercel

## 🔧 Troubleshooting

### Docker

**Проблема:** Контейнер не запускается
- Проверьте логи: `docker-compose logs`
- Убедитесь что порты свободны
- Проверьте права доступа к директориям

**Проблема:** Frontend не может подключиться к Backend
- Убедитесь что backend доступен на `http://localhost:4000`
- Проверьте переменную `VITE_API_BASE` в docker-compose.yml

### Vercel

**Проблема:** Сборка падает
- Проверьте что все зависимости указаны в `package.json`
- Убедитесь что `Root Directory` указан как `frontend`

**Проблема:** API запросы не работают
- Проверьте переменную `VITE_API_BASE` в настройках Vercel
- Убедитесь что CORS настроен на backend
- Проверьте что backend доступен из интернета

